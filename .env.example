# AdvoOS MVP — Umgebungsvariablen (serverseitig, NIE mit VITE_-Präfix → bleibt aus dem Browser)
#
# Ohne diese Werte läuft die Demo im MOCK-Modus (offline, kostenlos).
# Für echten KI-Dialog einen Schweizer, OpenAI-kompatiblen Anbieter eintragen.
#
# Beispiel Infomaniak AI (CH, OpenAI-kompatibel, kein Logging/Training):
#   AI_BASE_URL = https://api.infomaniak.com/1/ai/<PRODUCT_ID>/openai/v1
#   AI_API_KEY  = <dein-token>
#   AI_MODEL    = mixtral

AI_BASE_URL=
AI_API_KEY=
AI_MODEL=
