# --- Kern (Mock-Modus lauffähig, keine Modelle nötig) ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27              # LLM-Streaming (Hermes/vLLM) + MuseTalk-Sidecar

# --- Reale Modelle: siehe requirements-models.txt (pip install -r requirements-models.txt) ---
# vLLM (Hermes) läuft als eigener Container/Service, siehe docker-compose.yml.
# MuseTalk (Avatar) läuft als eigener GPU-Sidecar, siehe musetalk/.
